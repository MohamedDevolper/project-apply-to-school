import 'package:flutter/material.dart';

class SeetingsTabs extends StatelessWidget {
  const SeetingsTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
